# CSCE625Final

To run a sample dataset:
Download dataset from drive and move to project folder.
Install packages (perferably in a venv) using pip install -r requirements.txt
run Cnn.py
When prompted enter N to run 20 samples.
